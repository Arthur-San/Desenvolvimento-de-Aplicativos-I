<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qiflix">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QIFlix">
        <activity android:name=".MainShrek"></activity>
        <activity android:name=".MainPoze" />
        <activity android:name=".MainNegoNey" />
        <activity android:name=".MainMessi" />
        <activity android:name=".MainMenu" />
        <activity android:name=".subMenu.MainOutros" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ActivityLogin" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>